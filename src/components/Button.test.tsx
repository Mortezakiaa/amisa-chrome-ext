/* eslint-disable @typescript-eslint/no-unused-vars */
import { render } from "@testing-library/react";
import Button from "./Button";

describe("", () => {
  test("inti button", () => {
    render(<Button mode="default" txt="some" />);
  });
});
